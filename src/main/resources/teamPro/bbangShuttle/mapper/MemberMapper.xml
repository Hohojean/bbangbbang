<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamPro.bbangShuttle.mapper.MemberMapper">

    <select id="getUsers" resultType="MemberVO">
        select * from user;
    </select>

    <select id="selectOne" resultType="MemberVO">
        select * from user where userID=#{userID}
    </select>


<!--    <insert id="insertUser">-->
<!--        insert into user values( #{userID},#{userName},#{userEmail},#{userPwd},-->
<!--                                   #{userPhone},#{userAddr},#{userBirth},#{userGender} )-->
<!--    </insert>-->

</mapper>